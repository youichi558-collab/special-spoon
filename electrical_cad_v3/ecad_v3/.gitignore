__pycache__/
*.pyc
catalog_config.json
# 図面のバックアップ置き場(2026-09-20)。控えなのでコミットしない。
# 先頭の / は「このファイルと同じ階層の backup/ だけ」の意味。
# これが無いと、どの階層の backup/ にも当たってしまい、
# tools/backup/(バックアップ機能のコード本体)まで除外されて、
# pullしても機能が入っていない状態になる。
/backup/
