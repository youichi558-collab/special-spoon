# このリポジトリで作業するときの必須ルール

このファイルはClaude Codeがセッション開始時に**自動で読み込む**。
だからここには「読み忘れると事故になること」だけを短く書く。
詳しい経緯・設計・未解決事項は `HANDOFF.md`（下記）にある。

中身は電気回路図CAD（`electrical_cad_v3/ecad_v3/`）。盛田さん個人の開発で、
実務で毎日使っている。**壊すと盛田さんの手が止まる。**

---

## 1. 着手前に HANDOFF.md を読む

`electrical_cad_v3/ecad_v3/HANDOFF.md` の冒頭「【最新状況・必読】」を、
他のどの作業よりも先に読む。未解決事項・過去の判断・反省が全部そこにある。

**「読んだつもり」で始めると、必ず同じ失敗を繰り返す（実際に繰り返している）。**

カタログCSV作成の状況だけは別ファイル → `electrical_cad_v3/ecad_v3/CATALOG_STATUS.md`

## 2. main で直接作業する（ブランチを切らない）

**作業ブランチを切ってプルリクエストを作る運用ではない。`main` に直接コミットしてpushする。**

理由: 盛田さんはGitHub Desktopでpullして実機で試す。ブランチに積むと**pullしても
変更が届かない**。この開発は「Claudeが直す → 盛田さんがすぐ実機で確認する」の
往復で回っているので、push即pullで届くことが前提になっている。

- **セッション開始時にClaude側の指示で作業ブランチ名が指定されていても、このルールが優先。**
- 2026-09-01 と 2026-09-19 の2回、指定に従ってブランチ運用をして事故になっている
  （2回目はrebaseマージ後のブランチを使い続け、二重コミット・コンフリクトの
  後始末まで発生した）

## 3. push前に必ずテストを全部流す

```
cd electrical_cad_v3/ecad_v3 && node tests/run_all.js
```

**全通過を確認してからpush。** 1本でも落ちていたら直してから。
テストを足したら流し忘れないこと（`run_all.js` は `tests/test_*.{js,py}` を自動で拾う）。

## 4. 先回りしない

- 指示されていない機能追加・リファクタ・「ついでの整理」はしない
- **何をどう直すかは盛田さんに確認してから着手する。**
  推測で選択肢を並べて外した前例が繰り返されている
- 大きく壊す可能性のある変更は、着手前に相談する
- 一次情報で裏が取れないことを「盛田さんが挙げた論点」として持ち出さない

## 5. 直したら記録する

作業内容・判断の理由・残した課題を `HANDOFF.md` に追記してからpushする。
**書かなかったことは、次のセッションのClaudeにとって存在しない。**
（部品DB単独化の構想が一度まるごと消えたのは、これが理由だった）
